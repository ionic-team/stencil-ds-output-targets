// import stencil from 'unplugin-stencil/next';

/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack: (config, { isServer }) => {
  //   config.plugins.push(stencil({ isServer }))
  //   return config;
  // }
};

export default nextConfig;
